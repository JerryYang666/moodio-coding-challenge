"use client";

// ── Stub for the coding challenge ──────────────────────────────────────────
// "Send to Desktop" targets the authenticated canvas feature, which is out of
// scope for the isolated browse page. Renders nothing.
export default function SendToDesktopModal(_props: {
  isOpen?: boolean;
  onOpenChange?: (open: boolean) => void;
  desktopId?: string;
  assets?: unknown[];
}) {
  return null;
}
