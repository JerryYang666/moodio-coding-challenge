"use client";

// ── Stub for the coding challenge ──────────────────────────────────────────
// Language switching relies on the next-intl routing/cookie setup the isolated
// app omits (it ships English only). Renders nothing.
export function LanguageSwitch() {
  return null;
}
